# The new badger.

Badger V3: Actually stable.

## Creating a development environment

First, get a copy of the repo.
Following that, run `pnpm i` in both `/frontend` and `/api`.
Once both are finished, run `pnpm run dev` in the main repo. Concurrently should handle the dirty work.
